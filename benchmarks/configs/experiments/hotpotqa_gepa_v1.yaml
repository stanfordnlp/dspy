name: hotpotqa_gepa_v1
description: HotPotQA optimization using GEPA with context-aware QA

# Reference shared configs
dataset_ref: hotpotqa_standard
model_ref: qwen3_4b
optimizer_ref: gepa
program_ref: naive
logging_ref: logging

# Override only what's different from the baseline
logging_overrides:
  save_models: true