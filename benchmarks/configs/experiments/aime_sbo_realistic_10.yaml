name: aime_sbo_realistic_10
description: AIME SBO run with laptop-realistic settings and answer-only output

dataset_ref: aime_standard
model_ref: qwen3_4b
optimizer_ref: sbo_light
program_ref: math_answer_only
logging_ref: logging

dataset_overrides:
  train_size: 10
  dev_size: 3
  test_size: 3
  params:
    input_fields:
      - problem
    test_repeat: 1

model_overrides:
  params:
    max_tokens: 512

optimizer_overrides:
  params:
    max_iterations: 2
    max_null_steps: 2
    num_candidates: 3
    num_judge_samples: 3
    num_eval_samples: 2
    parse_failure_retries: 2
    parse_retry_temperature: 0.7
    max_critique_examples: 2
    max_critique_field_chars: 1600
    judge_max_tokens: 10
    proposer_max_tokens: 2048
    critic_max_tokens: 2048
    descent_param: 0.1
    lambda_init: 1.0
    lambda_min: 0.1
    lambda_max: 10.0
    lambda_gamma: 0.3
    tau_margin: 0.5
    temperature: 0.5
    judge_temperature: 0.7
    judge_cache: false
    proposer_temperature: 0.5
    critic_temperature: 0.5
    eval_temperature: 0.7
    eval_cache: false
    track_stats: true

logging_overrides:
  save_models: false
  level: INFO
