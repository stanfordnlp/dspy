name: aime_sbo_nontrivial
description: Moderate AIME full SBO run with multiple candidates, iterations, and dev examples

dataset_ref: aime_fast
model_ref: qwen3_4b
optimizer_ref: sbo_light
program_ref: math_answer_only
logging_ref: logging

model_overrides:
  params:
    max_tokens: 512

dataset_overrides:
  train_size: 6
  dev_size: 2
  test_size: 2
  params:
    input_fields:
      - problem
    test_repeat: 1

optimizer_overrides:
  params:
    max_iterations: 2
    max_null_steps: 2
    num_candidates: 2
    num_judge_samples: 2
    num_eval_samples: 1
    bundle_size: 6
    active_bundle_size: 2
    watchlist_size: 1
    parse_failure_retries: 2
    parse_retry_temperature: 0.7
    max_critique_examples: 2
    max_critique_field_chars: 1600
    judge_max_tokens: 10
    proposer_max_tokens: 2048
    critic_max_tokens: 2048
    temperature: 0.4
    judge_temperature: 0.7
    proposer_temperature: 0.4
    critic_temperature: 0.4
    eval_temperature: 0.7
    track_stats: true

logging_overrides:
  save_models: false
