name: aime_gepa_v1
description: AIME optimization using GEPA with math chain-of-thought

# Reference shared configs
dataset_ref: aime_standard
model_ref: qwen3_4b
optimizer_ref: gepa
program_ref: math_naive
logging_ref: logging

# Override logging to save models
logging_overrides:
  save_models: true

# Override GEPA settings for math problems (reduce minibatch size if needed)
optimizer_overrides:
  params:
    reflection_minibatch_size: 5  # Reduce from 3 to 2 for potentially long math problems
