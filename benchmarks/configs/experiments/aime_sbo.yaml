name: aime_sbo
description: AIME optimization using SBO with math chain-of-thought

# Reference shared configs
dataset_ref: aime_standard
model_ref: qwen3_4b
optimizer_ref: sbo_light
program_ref: math_cot
logging_ref: logging

# Realistic optimizer settings
optimizer_overrides:
  params:
    max_iterations: 100      # Enough iterations to see improvement
    num_candidates: 5       # Generate 3 variations per iteration
    num_judge_samples: 3    # 3 samples for judge averaging
    max_null_steps: 10       # Allow 5 consecutive null steps
    temperature: 0.7
    lambda_init: 1.0
    descent_param: 0.1      # Standard descent parameter

logging_overrides:
  save_models: true
  log_level: INFO
