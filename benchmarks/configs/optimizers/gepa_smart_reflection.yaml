name: gepa
auto: light
num_threads: 1
params:
  reflection_lm: ollama_chat/gemma3n:e2b  # Use larger model for instruction generation
  reflection_minibatch_size: 2  # Reduce batch size since reflection model is larger
  max_metric_calls: 1000
  track_stats: true

